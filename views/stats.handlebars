{{#if loggedIn}}
{{User.username}}

<div class="container text-center">
  <div class="row">
    <div class="col-6 col-sm-3">{{statistics.total_played}}</div>
    <div class="col-6 col-sm-3"> {{staticstics.wins}}</div>

    {{stats.graph}}
</div>
</div>


<button type="button" class="btn btn-outline-primary">Edit Account</button>
<button type="button" class="btn btn-outline-danger">Delete Account</button>
{{/if}}